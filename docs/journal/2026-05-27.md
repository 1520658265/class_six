# 2026-05-27

- 已补主角元生横版战斗资产：`assets/art/characters/yuansheng/battle/` 下按 body/weapons/effects 分类，采用“本体动作层 + 可替换武器层 + 特效层”。武器采用校园物件：空手、书包、扫帚、铝饭盒、悠悠球、黑板擦；所有运行时 PNG 已验证透明角。body v1 适合作战斗 pose 原型，后续仍需正式逐帧动画。

- 用户特别强调：后续所有主角战斗角色帧都必须可以使用现有这套可替换武器。角色本体帧不能把武器画死进去，必须预留统一 WeaponSlot/手部挂点，让书包、扫帚、铝饭盒、悠悠球、黑板擦都能套用。

- 用户补充新的资产处理约束：AI 生成图片后只做分类归档和命名整理，不要再切图、转 PNG、抠透明、缩放或进行其他图片本身处理；之前的切图/转换造成了分辨率降低。后续生成美术必须保留 AI 原图。

- 继续生成并归档角色原图：姐姐、父亲、母亲、门卫大爷、李静。归档到 `assets/art/characters/{jiejie,fuqin,muqin,menwei_daye,lijing}/raw/`，只复制原始 JPG，哈希校验与 `tools/ai/out/角色/` 原图一致。

- 继续生成并归档次要 NPC 原图：胡晓东、张磊、曹正东奶奶、曾建明爸爸、曾建明妈妈。归档到对应 `assets/art/characters/*/raw/`，只复制原始 JPG，哈希校验一致。

- 用户指出前面角色只有概念图，没有行走图。已停止继续补新角色，转为补核心 NPC 行走表原图。生成并归档：曹正东 v2、鲍师母 v2、王炎 v3 为当前可用 1024x1024 4x4 行走表；鲍先进 v2、曾建明 v2 归档为 reference，因为个别格子方向不稳。所有归档只复制原 JPG，不处理图片。

- 已继续补家庭线/校园配角行走表：姐姐、父亲、门卫大爷为当前可用；母亲、李静归档为 reference。李静这次模型输出为 2048x2048 PNG，但仍保持原图归档，不做处理。生成脚本 `tools/ai/gen_with_gemini.py` 已加入请求重试，避免代理偶发 `Response ended prematurely` 导致批量生成失败。

- 按“尽量不要返工”的原则，次要 NPC 行走表改为低变量策略：去掉手持道具，只保留眼镜、头巾、卷发、体态等识别点。结果：胡晓东当前可用；张磊、曹正东奶奶、曾建明妈妈、曾建明爸爸归档为 reference，不继续追着重生。曾建明爸爸虽然成功生成，但内容混排，不算可用行走表。

- 用户明确主角元生性格不走“自卑、敏感”路线，保留酷帅杀马特设定；后续主角视觉以斜刘海、红领巾、短袖短裤、黄色手环、要面子/爱装酷为基准。
- 已建立 AI prompt 中文分类目录：`tools/ai/prompts/角色精灵`、`场景建筑`、`地表自然`、`校园道具`、`参考临时`，新增 `tools/ai/prompts/README.md` 说明 prompt 中文命名、工程资产英文命名。
- 已制作主角元生第一批美术资产：`assets/art/characters/yuansheng/char_yuansheng_concept_v2.png`、`char_yuansheng_portraits_v1.png`、`char_yuansheng_walksheet_source_v1.png`，并切出 16 帧 32x32 透明行走帧到 `assets/art/characters/yuansheng/walk/`。
- 本轮新生成的 `tools/ai/out/角色/主角元生_行走表_v2.png` 和 `主角元生_行走表_v3.jpg` 不适合直接使用：v2 有白底和方向漂移，v3 变成 6 列；当前行走表采用旧的 `character_protagonist_cool_shamat_sheet_v1.jpg` 作为可用来源。
