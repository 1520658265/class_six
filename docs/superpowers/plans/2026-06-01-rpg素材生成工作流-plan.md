# RPG 素材生成工作流 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立一套可复用的 Liblib 素材生产管线，输入真实照片即可产出 HD 像素风 RPG 全套素材（立绘/头像/表情/行走图/战斗图/装备分图层）。

**Architecture:** 双产线（主角 LoRA 精调 + NPC IP-Adapter 模板化），六阶段流水线（扩样本 → LoRA 训练 → 立绘 → 行走图 → 战斗图 → 表情头像），所有动画素材分图层输出并通过锚点系统对齐。

**Tech Stack:** Liblib（Stable Diffusion XL / Pony V6）、ControlNet OpenPose/Canny、IP-Adapter Plus Face / InstantID、Python（辅助脚本）

**Spec:** `docs/design/rpg素材生成工作流-spec.md`

---

## File Structure

```
production/rpg-pipeline/
├── prompts/
│   ├── base-style.txt                    ← 通用正向风格 prompt
│   ├── negative-prompt.txt               ← 通用负向 prompt
│   ├── hero/
│   │   ├── portrait.txt                  ← 主角立绘 prompt 模板
│   │   ├── walk.txt                      ← 主角行走图 prompt 模板
│   │   ├── battle-idle.txt               ← 战斗待机
│   │   ├── battle-walk-in.txt
│   │   ├── battle-attack.txt
│   │   ├── battle-charge.txt
│   │   ├── battle-skill.txt
│   │   ├── battle-guard.txt
│   │   ├── battle-dodge.txt
│   │   ├── battle-hurt.txt
│   │   ├── battle-critical-hurt.txt
│   │   ├── battle-low-hp.txt
│   │   ├── battle-dead.txt
│   │   ├── battle-victory.txt
│   │   ├── expression.txt                ← 表情差分 prompt 模板
│   │   └── face-icon.txt                 ← 头像 prompt 模板
│   ├── npc/
│   │   ├── portrait-template.txt
│   │   ├── walk-template.txt
│   │   └── battle-template.txt
│   └── controlnet/
│       └── README.md                     ← 骨架模板需求说明
├── tools/
│   ├── validate_naming.py                ← 命名规范校验
│   └── assemble_spritesheet.py           ← 单帧拼接为 sprite sheet
├── sop/
│   └── production-guide.md               ← 端到端操作手册
└── assets/                               ← 生成素材存放（gitignore）
    └── .gitkeep
```

---

## Tasks

---

### Task 1: 创建目录骨架

**Files:**
- Create: `production/rpg-pipeline/prompts/base-style.txt`
- Create: `production/rpg-pipeline/prompts/negative-prompt.txt`
- Create: `production/rpg-pipeline/prompts/hero/` (directory)
- Create: `production/rpg-pipeline/prompts/npc/` (directory)
- Create: `production/rpg-pipeline/prompts/controlnet/` (directory)
- Create: `production/rpg-pipeline/tools/` (directory)
- Create: `production/rpg-pipeline/sop/` (directory)
- Create: `production/rpg-pipeline/assets/.gitkeep`
- Create: `production/rpg-pipeline/assets/.gitignore`

- [ ] **Step 1: 创建完整目录结构**

```powershell
$base = "D:\PersonalProject\class_six\production\rpg-pipeline"
$dirs = @(
    "$base\prompts\hero",
    "$base\prompts\npc",
    "$base\prompts\controlnet",
    "$base\tools",
    "$base\sop",
    "$base\assets\characters"
)
foreach ($d in $dirs) { New-Item -ItemType Directory -Force $d }
```

- [ ] **Step 2: 创建 assets/.gitignore**

```gitignore
# 生成素材不入库（体积大），仅保留目录结构
*
!.gitignore
!.gitkeep
!characters/
!characters/**/
!characters/**/.gitkeep
```

- [ ] **Step 3: 创建 assets/.gitkeep 占位**

空文件，确保 git 追踪目录。

- [ ] **Step 4: 验收**

运行 `Get-ChildItem -Recurse production/rpg-pipeline` 确认目录树与 File Structure 一致。

- [ ] **Step 5: 建议提交节点**

```
feat: scaffold rpg-pipeline directory structure
```

---

### Task 2: 通用风格 Prompt 模板

**Files:**
- Create: `production/rpg-pipeline/prompts/base-style.txt`
- Create: `production/rpg-pipeline/prompts/negative-prompt.txt`

- [ ] **Step 1: 写 base-style.txt**

```text
masterpiece, best quality, pixel art, HD-2D style, high resolution pixel art,
octopath traveler style, triangle strategy style,
(limited color palette:1.2), (crisp pixel edges:1.3), cel shading, bold outline,
detailed pixel shading, 16-bit detailed style,
no anti-aliasing, sharp edges, clean pixel boundaries
```

- [ ] **Step 2: 写 negative-prompt.txt**

```text
realistic, photorealistic, photo, 3d, octane, unreal engine, cgi,
blurry, anti-aliasing, smooth shading, soft lighting, depth of field,
nsfw, lowres, bad anatomy, bad hands, missing fingers, extra fingers,
watermark, signature, text, logo, jpeg artifacts,
multiple characters, duplicate, frame inconsistency,
oil painting, watercolor, sketch, pencil drawing,
low resolution pixels, retro 8-bit, NES style, gameboy style
```

- [ ] **Step 3: 验收**

确认两个文件内容完整，无拼写错误。base-style 包含 HD-2D 关键词，negative 排除了写实和低分辨率像素。

- [ ] **Step 4: 建议提交节点**

```
feat: add base style and negative prompt templates
```

---

### Task 3: 主角立绘 Prompt 模板

**Files:**
- Create: `production/rpg-pipeline/prompts/hero/portrait.txt`

- [ ] **Step 1: 写 portrait.txt**

```text
# 主角立绘 Prompt 模板
# 使用方式：将 {变量} 替换为实际值，与 base-style.txt 拼接后作为正向 prompt

# === 正向 Prompt ===
{base-style.txt 内容},
solo, 1boy/1girl, full body, standing pose, 3/4 front view,
{角色描述：发色/发型/肤色/体型},
{服装描述：具体装备组合},
{武器描述：持剑/持弓/持杖等},
fantasy rpg adventurer, detailed pixel character portrait,
plain gradient background, centered composition,
(character design sheet:1.1),
<lora:pixel_art_hd:0.9> <lora:char_{角色名}:0.8>

# === Liblib 参数 ===
# 底模: SDXL 1.0 / Pony V6
# 尺寸: 832x1216 或 1024x1536
# 采样器: DPM++ 2M Karras
# 步数: 30
# CFG: 6-7
# Seed: {角色固定 seed}
# IP-Adapter: 关（主角用 LoRA）
# ControlNet: 关

# === 使用示例 ===
# 角色: hero01（男性战士）
# 正向: masterpiece, best quality, pixel art, HD-2D style, ...,
#        solo, 1boy, full body, standing pose, 3/4 front view,
#        short black hair, tan skin, lean build,
#        light leather armor, brown cloak, belt with pouches,
#        holding longsword in right hand,
#        fantasy rpg adventurer, detailed pixel character portrait,
#        plain gradient background, centered composition,
#        <lora:pixel_art_hd:0.9> <lora:char_hero01:0.8>
```

- [ ] **Step 2: 验收**

确认模板包含：变量占位符、Liblib 参数说明、使用示例。

- [ ] **Step 3: 建议提交节点**

```
feat: add hero portrait prompt template
```

---

### Task 4: 主角行走图 Prompt 模板

**Files:**
- Create: `production/rpg-pipeline/prompts/hero/walk.txt`

- [ ] **Step 1: 写 walk.txt**

```text
# 主角行走图 Prompt 模板（单帧生成，共 16 帧）
# 使用方式：每帧单独生成，变量替换后与 base-style.txt 拼接
# 生成顺序：先出 4 方向中立帧（frame 1），再出迈步帧

# === 正向 Prompt（单帧） ===
{base-style.txt 内容},
solo, 1boy/1girl, {角色描述},
{服装描述 / "base layer, minimal clothing" 用于基础层},
chibi proportions, 3-head-tall, top-down 45 degree rpg view,
walking animation frame, {方向描述}, {步态描述},
single character sprite, centered in frame, full body,
(transparent background:1.3), (single frame:1.2),
<lora:pixel_art_hd:0.9> <lora:char_{角色名}:0.8>

# === 方向描述变量 ===
# down:  facing viewer, front view
# left:  facing left, side view
# right: facing right, side view
# up:    facing away from viewer, back view

# === 步态描述变量 ===
# frame1: standing neutral, feet together (中立帧)
# frame2: left foot forward, mid-stride (左脚迈步)
# frame3: standing neutral, feet together (中立帧，同 frame1)
# frame4: right foot forward, mid-stride (右脚迈步)

# === Liblib 参数 ===
# 底模: SDXL 1.0 / Pony V6
# 尺寸: 512x512（生成后裁切缩放至 128x128）
# 采样器: DPM++ 2M Karras
# 步数: 30
# CFG: 6-7
# Seed: {角色固定 seed}（全 16 帧同一 seed）
# ControlNet: OpenPose, 权重 0.8-1.0
#   - 模板图: controlnet/walk-{方向}-{帧号}.png
# IP-Adapter: 关（主角用 LoRA）

# === 图层说明 ===
# 基础层: 服装描述改为 "base layer, minimal clothing, underwear"
# 装备层: 服装描述改为具体装备，背景必须透明
#          prompt 追加: (only equipment visible:1.3), (no body:1.2)

# === 命名规范 ===
# {角色}_walk_{方向}_{帧号}_base.png
# {角色}_walk_{方向}_{帧号}_{装备名}.png
```

- [ ] **Step 2: 验收**

确认模板覆盖：4 方向 × 4 帧 = 16 种组合的变量说明、图层分离说明、ControlNet 配置。

- [ ] **Step 3: 建议提交节点**

```
feat: add hero walk sprite prompt template
```

---

### Task 5: 战斗图 Prompt 模板（12 动作）

**Files:**
- Create: `production/rpg-pipeline/prompts/hero/battle-idle.txt`
- Create: `production/rpg-pipeline/prompts/hero/battle-walk-in.txt`
- Create: `production/rpg-pipeline/prompts/hero/battle-attack.txt`
- Create: `production/rpg-pipeline/prompts/hero/battle-charge.txt`
- Create: `production/rpg-pipeline/prompts/hero/battle-skill.txt`
- Create: `production/rpg-pipeline/prompts/hero/battle-guard.txt`
- Create: `production/rpg-pipeline/prompts/hero/battle-dodge.txt`
- Create: `production/rpg-pipeline/prompts/hero/battle-hurt.txt`
- Create: `production/rpg-pipeline/prompts/hero/battle-critical-hurt.txt`
- Create: `production/rpg-pipeline/prompts/hero/battle-low-hp.txt`
- Create: `production/rpg-pipeline/prompts/hero/battle-dead.txt`
- Create: `production/rpg-pipeline/prompts/hero/battle-victory.txt`

- [ ] **Step 1: 写 battle-idle.txt（作为所有战斗 prompt 的基础模板）**

```text
# 战斗图 Prompt 模板 — 待机 idle（4 帧循环）
# 使用方式：每帧单独生成，与 base-style.txt 拼接

# === 正向 Prompt（单帧） ===
{base-style.txt 内容},
solo, 1boy/1girl, {角色描述},
{服装描述 / "base layer" 用于基础层},
battle stance, idle animation frame,
side view, facing right, rpg battle scene perspective,
{帧描述},
single character sprite, centered in frame, full body,
(transparent background:1.3), (single frame:1.2),
<lora:pixel_art_hd:0.9> <lora:char_{角色名}:0.8>

# === 帧描述变量（4 帧呼吸循环） ===
# frame1: neutral standing, arms at sides, relaxed
# frame2: slight inhale, chest raised slightly
# frame3: neutral standing, same as frame1
# frame4: slight exhale, shoulders drop slightly

# === Liblib 参数 ===
# 底模: SDXL 1.0 / Pony V6
# 尺寸: 768x768（生成后裁切缩放至 256x256）
# 采样器: DPM++ 2M Karras
# 步数: 30
# CFG: 6-7
# Seed: {角色固定 seed}
# ControlNet: OpenPose, 权重 0.8
#   - 模板图: controlnet/battle-idle-{帧号}.png

# === 图层说明 ===
# 基础层: 服装改为 "base layer, minimal clothing"
# 武器层: prompt 改为 "{武器描述}, (only weapon visible:1.3), (no body:1.2)"
```

- [ ] **Step 2: 写其余 11 个战斗动作 prompt**

每个文件结构与 battle-idle.txt 相同，仅替换：
- 动作描述关键词
- 帧描述变量（帧数和内容）
- ControlNet 模板图路径

各动作核心差异：

| 文件 | 动作关键词 | 帧数 | 帧描述要点 |
|---|---|---|---|
| battle-walk-in | walking forward, approaching | 4 | 迈步接近 |
| battle-attack | sword slash, attacking | 6 | 起手→挥击→收招 |
| battle-charge | charging energy, power up | 6 | 蓄力姿态→释放 |
| battle-skill | casting spell, magic release | 8 | 咏唱→释放→余波 |
| battle-guard | blocking, defensive stance | 3 | 举盾格挡 |
| battle-dodge | dodging, evading, side step | 4 | 侧身闪避 |
| battle-hurt | hit reaction, knocked back | 3 | 被击退 |
| battle-critical-hurt | heavy hit, falling back | 4 | 大幅后仰 |
| battle-low-hp | exhausted, kneeling, panting | 4 | 弯腰喘气循环 |
| battle-dead | collapsing, fallen | 5 | 倒地序列 |
| battle-victory | celebrating, victory pose | 6 | 胜利动作 |

- [ ] **Step 3: 验收**

确认 12 个文件全部存在，每个文件包含：正向 prompt 模板、帧描述变量、Liblib 参数、图层说明。

- [ ] **Step 4: 建议提交节点**

```
feat: add all 12 battle action prompt templates
```

---

### Task 6: 表情差分 + 头像 Prompt 模板

**Files:**
- Create: `production/rpg-pipeline/prompts/hero/expression.txt`
- Create: `production/rpg-pipeline/prompts/hero/face-icon.txt`

- [ ] **Step 1: 写 expression.txt**

```text
# 表情差分 Prompt 模板（Inpainting 模式）
# 使用方式：以头像图为底图，蒙版圈住面部五官区域，仅重绘表情

# === Inpainting 正向 Prompt ===
{base-style.txt 内容},
close-up face, portrait, {角色描述：发色/发型/肤色},
{表情描述},
pixel art face, detailed pixel eyes, HD-2D style,
<lora:pixel_art_hd:0.9> <lora:char_{角色名}:0.8>

# === 表情描述变量（6-8 种） ===
# neutral:  calm expression, relaxed face, neutral mouth
# happy:    smiling, bright eyes, open mouth smile
# angry:    furrowed brows, gritted teeth, intense eyes
# sad:      downcast eyes, slight frown, melancholy
# surprised: wide eyes, open mouth, raised eyebrows
# shy:      blushing cheeks, averted gaze, slight smile
# hurt:     pained expression, one eye closed, sweat drop
# eyes_closed: closed eyes, peaceful expression, sleeping

# === Liblib Inpainting 参数 ===
# 模式: Inpainting（局部重绘）
# 蒙版区域: 仅面部五官（眉毛到下巴，不含发型和轮廓）
# 重绘幅度: 0.5-0.65（太高会改变脸型）
# 底模: 与原图一致
# 尺寸: 与原图一致（256x256）
# Seed: {角色固定 seed}
# 步数: 25-30
# CFG: 6

# === 命名规范 ===
# {角色}_expression_{表情名}.png
# 例: hero01_expression_happy.png
```

- [ ] **Step 2: 写 face-icon.txt**

```text
# 头像 Prompt 模板
# 使用方式：从立绘裁切上半身后，用 img2img 精修为正方形头像

# === img2img 正向 Prompt ===
{base-style.txt 内容},
close-up portrait, head and shoulders, centered face,
{角色描述：发色/发型/肤色/五官},
pixel art portrait icon, rpg character icon, HD-2D style,
simple background, (square composition:1.2),
<lora:pixel_art_hd:0.9> <lora:char_{角色名}:0.8>

# === Liblib 参数 ===
# 模式: img2img（图生图）
# 输入图: 从立绘裁切的上半身区域
# 重绘幅度: 0.3-0.4（仅微调，保持与立绘一致）
# 尺寸: 256x256
# Seed: {角色固定 seed}

# === 命名规范 ===
# {角色}_face.png
```

- [ ] **Step 3: 验收**

确认两个模板覆盖：8 种表情变量、Inpainting 参数、img2img 参数、命名规范。

- [ ] **Step 4: 建议提交节点**

```
feat: add expression and face-icon prompt templates
```

---

### Task 7: NPC 批量 Prompt 模板

**Files:**
- Create: `production/rpg-pipeline/prompts/npc/portrait-template.txt`
- Create: `production/rpg-pipeline/prompts/npc/walk-template.txt`
- Create: `production/rpg-pipeline/prompts/npc/battle-template.txt`

- [ ] **Step 1: 写 portrait-template.txt**

```text
# NPC 立绘 Prompt 模板（IP-Adapter 模式，无 LoRA）
# 使用方式：上传 NPC 照片到 IP-Adapter Face，配合本模板生成

# === 正向 Prompt ===
{base-style.txt 内容},
solo, 1boy/1girl, full body, standing pose, 3/4 front view,
{NPC 职业/身份描述},
{NPC 服装描述},
fantasy rpg npc, detailed pixel character,
plain gradient background, centered composition,
<lora:pixel_art_hd:0.9>

# === Liblib 参数 ===
# 底模: SDXL 1.0 / Pony V6
# 尺寸: 832x1216
# IP-Adapter: Plus Face, 权重 0.6-0.7
# LoRA: 仅像素风 LoRA，无角色 LoRA
# Seed: 随机（每个 NPC 不同）
# 采样器: DPM++ 2M Karras
# 步数: 30, CFG: 6-7

# === 批量技巧 ===
# 1. 固定 base prompt，仅替换职业/服装描述
# 2. IP-Adapter 权重 0.6 = 保留五官但允许风格化
# 3. 每 5-10 张检查一次风格一致性
# 4. 发现漂移立即停产，检查 LoRA 权重
```

- [ ] **Step 2: 写 walk-template.txt（NPC 简化版行走图）**

```text
# NPC 行走图 Prompt 模板（简化版：固定模板 + 换脸换色）
# NPC 行走图共用 2-3 套骨架模板，仅替换外观

# === 正向 Prompt ===
{base-style.txt 内容},
solo, 1boy/1girl, {NPC 外观描述},
chibi proportions, 3-head-tall, top-down 45 degree rpg view,
walking animation frame, {方向描述}, {步态描述},
single character sprite, centered in frame, full body,
(transparent background:1.3),
<lora:pixel_art_hd:0.9>

# === Liblib 参数 ===
# IP-Adapter: Plus Face, 权重 0.5（行走图小尺寸，脸部权重降低）
# ControlNet: OpenPose, 权重 0.9（NPC 用更高权重锁姿态）
# Seed: 固定（同一 NPC 全 16 帧同 seed）
# 尺寸: 512x512

# === 模板复用策略 ===
# 模板 A: 普通行走（适用大部分 NPC）
# 模板 B: 持物行走（商人、挑担等）
# 模板 C: 老人/儿童行走（步幅小）
# 选择模板后仅替换外观描述和 IP-Adapter 参考图
```

- [ ] **Step 3: 写 battle-template.txt（NPC 简化版战斗图）**

```text
# NPC 战斗图 Prompt 模板（简化版：仅 6 核心动作）
# NPC 战斗图精简为：待机/攻击/受伤/死亡/技能/胜利

# === 正向 Prompt ===
{base-style.txt 内容},
solo, 1boy/1girl, {NPC 外观描述},
battle stance, {动作描述},
side view, facing right, rpg battle scene perspective,
single character sprite, centered in frame, full body,
(transparent background:1.3),
<lora:pixel_art_hd:0.9>

# === NPC 战斗动作（精简 6 组 / 25 帧） ===
# idle: 4 帧
# attack: 6 帧
# skill: 6 帧
# hurt: 3 帧
# dead: 4 帧
# victory: 2 帧

# === Liblib 参数 ===
# IP-Adapter: Plus Face, 权重 0.5
# ControlNet: OpenPose, 权重 0.8
# Seed: 固定
# 尺寸: 768x768
```

- [ ] **Step 4: 验收**

确认 3 个 NPC 模板文件完整，包含 IP-Adapter 配置（区别于主角的 LoRA 方式）、批量技巧、模板复用策略。

- [ ] **Step 5: 建议提交节点**

```
feat: add NPC batch prompt templates
```

---

### Task 8: ControlNet 骨架模板需求说明

**Files:**
- Create: `production/rpg-pipeline/prompts/controlnet/README.md`

- [ ] **Step 1: 写 README.md**

```markdown
# ControlNet 骨架模板需求

本目录存放行走图和战斗图的 ControlNet 参考骨架图。
骨架图需要手动制作或从现有素材提取，不由 AI 自动生成。

## 行走图骨架（16 张）

| 文件名 | 方向 | 帧 | 姿态描述 |
|---|---|---|---|
| walk-down-1.png | 朝下 | 中立 | 双脚并立，面向镜头 |
| walk-down-2.png | 朝下 | 左脚迈步 | 左脚前伸，重心前移 |
| walk-down-3.png | 朝下 | 中立 | 同 frame1 |
| walk-down-4.png | 朝下 | 右脚迈步 | 右脚前伸，重心前移 |
| walk-left-1~4.png | 朝左 | 同上 | 侧面视角 |
| walk-right-1~4.png | 朝右 | 同上 | 侧面视角（可镜像 left） |
| walk-up-1~4.png | 朝上 | 同上 | 背面视角 |

## 战斗图骨架（57 张）

每个动作的每一帧各需一张骨架图：

| 动作 | 帧数 | 文件名模式 |
|---|---|---|
| idle | 4 | battle-idle-1~4.png |
| walk-in | 4 | battle-walk-in-1~4.png |
| attack | 6 | battle-attack-1~6.png |
| charge | 6 | battle-charge-1~6.png |
| skill | 8 | battle-skill-1~8.png |
| guard | 3 | battle-guard-1~3.png |
| dodge | 4 | battle-dodge-1~4.png |
| hurt | 3 | battle-hurt-1~3.png |
| critical-hurt | 4 | battle-critical-hurt-1~4.png |
| low-hp | 4 | battle-low-hp-1~4.png |
| dead | 5 | battle-dead-1~5.png |
| victory | 6 | battle-victory-1~6.png |

## 制作方式（三选一）

1. **手绘骨架**：用 Aseprite / Piskel 画火柴人骨架，导出 PNG
2. **从现有素材提取**：找一套免费 RPG sprite，用 OpenPose 提取骨架
3. **3D 工具导出**：用 MagicaVoxel / Blender 摆 pose 后渲染为骨架线稿

## 规格要求

- 尺寸：与目标生成尺寸一致（行走 512×512，战斗 768×768）
- 格式：PNG，白底黑线 或 OpenPose 标准彩色关节图
- ControlNet 类型：OpenPose（推荐）或 Canny
- 所有帧的人物位置/大小必须一致（锚点对齐）
```

- [ ] **Step 2: 验收**

确认 README 覆盖：行走 16 帧 + 战斗 57 帧的完整文件名清单、三种制作方式、规格要求。

- [ ] **Step 3: 建议提交节点**

```
feat: add controlnet template requirements doc
```

---

### Task 9: 命名规范校验脚本

**Files:**
- Create: `production/rpg-pipeline/tools/validate_naming.py`

- [ ] **Step 1: 写校验脚本**

```python
"""
RPG 素材命名规范校验器
规范: {角色}_{动作}_{方向}_{帧号}_{图层}.png

用法: python validate_naming.py <assets_directory>
"""
import sys
import re
from pathlib import Path

VALID_ACTIONS = [
    "walk", "idle", "walk-in", "attack", "charge", "skill",
    "guard", "dodge", "hurt", "critical-hurt", "low-hp",
    "dead", "victory", "expression", "face", "portrait"
]
VALID_DIRECTIONS = ["down", "left", "right", "up", "front", "side", "back"]
VALID_LAYERS = ["base", "weapon-.*", "armor-.*", "helm-.*", "accessory-.*"]

PATTERN = re.compile(
    r"^([a-z0-9]+)_([a-z\-]+)_([a-z]+)_(\d{2})_([a-z\-]+)\.png$"
)

def validate_file(filepath: Path) -> list[str]:
    errors = []
    name = filepath.name

    if name.startswith("."):
        return []

    match = PATTERN.match(name)
    if not match:
        errors.append(f"命名格式错误: {name}")
        return errors

    _, action, direction, frame, layer = match.groups()

    if action not in VALID_ACTIONS:
        errors.append(f"未知动作 '{action}': {name}")
    if direction not in VALID_DIRECTIONS:
        errors.append(f"未知方向 '{direction}': {name}")
    if not any(re.match(p, layer) for p in VALID_LAYERS):
        errors.append(f"未知图层 '{layer}': {name}")

    return errors

def main():
    if len(sys.argv) < 2:
        print("用法: python validate_naming.py <assets_directory>")
        sys.exit(1)

    target = Path(sys.argv[1])
    if not target.is_dir():
        print(f"错误: {target} 不是有效目录")
        sys.exit(1)

    all_errors = []
    for png in target.rglob("*.png"):
        errors = validate_file(png)
        all_errors.extend(errors)

    if all_errors:
        print(f"发现 {len(all_errors)} 个命名错误:")
        for e in all_errors:
            print(f"  - {e}")
        sys.exit(1)
    else:
        print("所有文件命名规范检查通过")

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 验收**

运行 `python validate_naming.py production/rpg-pipeline/assets/`（空目录应输出"通过"）。

- [ ] **Step 3: 建议提交节点**

```
feat: add asset naming validation script
```

---

### Task 10: Sprite Sheet 拼接脚本

**Files:**
- Create: `production/rpg-pipeline/tools/assemble_spritesheet.py`

- [ ] **Step 1: 写拼接脚本**

```python
"""
将单帧 PNG 拼接为 sprite sheet。
用法: python assemble_spritesheet.py <input_dir> <output.png> --cols 4 --rows 4

输入目录中的文件按文件名排序后按行优先填入网格。
"""
import sys
import argparse
from pathlib import Path

try:
    from PIL import Image
except ImportError:
    print("需要安装 Pillow: pip install Pillow")
    sys.exit(1)


def assemble(input_dir: Path, output: Path, cols: int, rows: int):
    frames = sorted(input_dir.glob("*.png"))
    expected = cols * rows
    if len(frames) != expected:
        print(f"警告: 期望 {expected} 帧，实际找到 {len(frames)} 帧")

    if not frames:
        print("错误: 输入目录中没有 PNG 文件")
        sys.exit(1)

    sample = Image.open(frames[0])
    fw, fh = sample.size

    sheet = Image.new("RGBA", (fw * cols, fh * rows), (0, 0, 0, 0))

    for i, frame_path in enumerate(frames):
        if i >= expected:
            break
        img = Image.open(frame_path).convert("RGBA")
        if img.size != (fw, fh):
            print(f"警告: {frame_path.name} 尺寸 {img.size} 与首帧 {(fw, fh)} 不一致，已缩放")
            img = img.resize((fw, fh), Image.NEAREST)
        col = i % cols
        row = i // cols
        sheet.paste(img, (col * fw, row * fh))

    sheet.save(output)
    print(f"拼接完成: {output} ({cols}x{rows}, 单帧 {fw}x{fh})")


def main():
    parser = argparse.ArgumentParser(description="单帧 PNG 拼接为 sprite sheet")
    parser.add_argument("input_dir", help="包含单帧 PNG 的目录")
    parser.add_argument("output", help="输出 sprite sheet 路径")
    parser.add_argument("--cols", type=int, default=4, help="列数（默认 4）")
    parser.add_argument("--rows", type=int, default=4, help="行数（默认 4）")
    args = parser.parse_args()

    assemble(Path(args.input_dir), Path(args.output), args.cols, args.rows)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 验收**

```powershell
python production/rpg-pipeline/tools/assemble_spritesheet.py --help
```

确认输出帮助信息，无 import 错误（需要 `pip install Pillow`）。

- [ ] **Step 3: 建议提交节点**

```
feat: add sprite sheet assembly script
```

---

### Task 11: 端到端操作手册（SOP）

**Files:**
- Create: `production/rpg-pipeline/sop/production-guide.md`

- [ ] **Step 1: 写 production-guide.md**

```markdown
# RPG 素材生产操作手册

## 前置准备

### 环境
- Liblib 账号（在线生图权限）
- Python 3.10+（辅助脚本）
- Pillow 库：`pip install Pillow`

### 素材准备
- 角色参考照片（正面清晰，光线均匀）
- ControlNet 骨架模板图（见 prompts/controlnet/README.md）
- 确认 Liblib 上可用的 LoRA：搜索 "pixel art"、"HD-2D"、"octopath"

---

## 主角生产流程（6 阶段）

### Stage 1：扩样本

1. 进入 Liblib → 在线生图 → img2img 模式
2. 上传角色照片
3. 挂载 IP-Adapter Plus Face，权重 0.7
4. 正向 prompt：使用 `prompts/base-style.txt` + 角度描述
5. 生成 20-30 张不同角度/光照/表情的同一人物图
6. 保存到 `assets/characters/{角色}/lora-samples/`

**验收标准**：20+ 张图，人物五官一致，角度覆盖正/侧/背/3-4

### Stage 2：训练角色 LoRA

1. 进入 Liblib → LoRA 训练
2. 上传 Stage 1 的扩样本
3. 底模选择：与最终生图一致（SDXL / Pony V6）
4. 触发词设为：`char_{角色名}`
5. 训练完成后下载/记录 LoRA ID
6. 保存到 `assets/characters/{角色}/lora-model/`

**验收标准**：用触发词 + 简单 prompt 生成 3 张测试图，确认人物一致

### Stage 3：立绘

1. 打开 `prompts/hero/portrait.txt`
2. 替换变量：角色描述、服装、武器
3. 拼接 base-style.txt 内容到正向 prompt 开头
4. 设置 Liblib 参数（见模板内说明）
5. 每套武器组合生成 3-5 张，选最佳
6. 保存到 `assets/characters/{角色}/portraits/`

**验收标准**：风格为 HD 像素风，人物与照片相似度 > 70%

### Stage 4：行走图

1. 打开 `prompts/hero/walk.txt`
2. 挂载 ControlNet OpenPose + 对应骨架模板图
3. 按 4 方向 × 4 帧 = 16 次生成
4. 基础层和装备层分别生成（见模板内图层说明）
5. 保存到 `assets/characters/{角色}/walk/base/` 和 `walk/equipment/`
6. 运行命名校验：
   `python tools/validate_naming.py assets/characters/{角色}/walk/`
7. 拼接 sprite sheet：
   `python tools/assemble_spritesheet.py assets/characters/{角色}/walk/base/ output/walk-base.png --cols 4 --rows 4`

**验收标准**：16 帧人物一致，帧间无明显跳变，装备层与基础层对齐

### Stage 5：战斗图

1. 打开 `prompts/hero/battle-{动作}.txt`
2. 挂载 ControlNet OpenPose + 对应动作骨架模板
3. 按每个动作的帧数逐帧生成
4. 基础层和武器层分别生成
5. 保存到 `assets/characters/{角色}/battle/{动作}/base/` 和 `battle/{动作}/equipment/`
6. 运行命名校验
7. 每个动作单独拼接 sprite sheet

**验收标准**：动作流畅，武器层与手部对齐

### Stage 6：表情差分 + 头像

1. 从立绘裁切上半身作为头像底图
2. 用 img2img 微调为 256×256 正方形头像
3. 用 Inpainting 模式 + `prompts/hero/expression.txt` 生成表情差分
4. 保存到 `assets/characters/{角色}/face/` 和 `expressions/`

**验收标准**：表情差分仅面部变化，轮廓/发型/服装不变

---

## NPC 批量生产流程

1. 准备 NPC 照片
2. 选择模板类型（普通/持物/老人儿童）
3. 使用 `prompts/npc/` 下对应模板
4. IP-Adapter Face 权重 0.6，无需训 LoRA
5. 每 5-10 个 NPC 检查一次风格一致性
6. 发现漂移立即停产排查

---

## 常见问题排查

| 现象 | 原因 | 解决 |
|---|---|---|
| 出来还是真人脸 | IP-Adapter 权重太高 | 降到 0.4-0.5 |
| 完全不像本人 | LoRA 权重太低或未加载 | 检查触发词，权重提到 0.8 |
| 不是像素风 | 像素 LoRA 权重不够 | 提到 1.0，检查底模 |
| 帧间人物不一致 | seed 未锁定 | 全程同一 seed |
| 装备层错位 | ControlNet 骨架不同 | 确认所有帧用同一骨架 |
| 风格突然变 3D | 负向 prompt 缺失 | 检查 negative-prompt.txt 是否完整 |
```

- [ ] **Step 2: 验收**

通读 SOP，确认覆盖 6 阶段 + NPC 流程 + 排查表，无遗漏步骤。

- [ ] **Step 3: 建议提交节点**

```
feat: add end-to-end production SOP guide
```

---

## Self-Review Checklist

- [x] **Spec 覆盖**：§1 目标 → Task 全覆盖；§2 素材清单 → Task 3-6 prompt 模板；§3 双产线 → Task 3-6（主角）+ Task 7（NPC）；§4 Liblib 配置 → 每个 prompt 模板内含参数；§5 六阶段 → Task 11 SOP；§6 分图层 → Task 4/5 图层说明 + Task 9 命名校验；§7 一致性 → 各模板 seed 锁定说明；§8 目录 → Task 1；§9 风险 → Task 11 排查表
- [x] **无占位符**：所有 prompt 模板包含完整内容和使用示例
- [x] **类型一致**：命名规范 `{角色}_{动作}_{方向}_{帧号}_{图层}.png` 在 Task 4/5/9/10/11 中一致
- [x] **无遗漏**：ControlNet 模板需求（Task 8）、辅助脚本（Task 9/10）、SOP（Task 11）全部覆盖
